[tool.ruff]
line-length = 120
target-version = "py39"
src = ["backend"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "SIM"]
ignore = ["B008", "E501"]

[tool.ruff.lint.per-file-ignores]
"backend/tests/*.py" = ["E402"]
